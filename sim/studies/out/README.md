# 측정 산출물

- `out/*.txt` — **`÷ s(n)` 반영 후**(2026-09-18) 실행 결과. 문서의 공표 수치는 전부 여기서 나온다.
- `out/before/` — `÷ s(n)` 반영 전 결과. 대비용으로만 보관한다. **인용하지 말 것.**

재현: `sim/studies/` 에서 `python3 <스크립트>.py`. 시드 20260910 · 반복 400회.
`mcompare.py` 는 15분 이상 걸린다.
